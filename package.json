{
  "name": "book-companion",
  "version": "1.0.0",
  "description": "AI-powered book companion with iMessage-style interface",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js",
    "dev": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
